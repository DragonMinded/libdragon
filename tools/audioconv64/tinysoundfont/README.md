TinySoundFont (vendored)
========================

Pinned copy of https://github.com/schellingb/TinySoundFont (MIT).

Used only by audioconv64 to parse SF2 banks when converting to SF64.
Do not use at N64 runtime.

Update by replacing `tsf.h` from upstream `master` and keeping this LICENSE.
